This project is an extremely basic proof of concept
just to show EntityFrameworkCore work in MAUI applications

All it does is to bind the Course table from the database
to a GUI component so the GUI component can display the
contents automatically. No interaction.

